Run main script with `mvn clean package -Dskiptests && java -jar target/unit-test-practice-1.0.jar`.

Run all tests with `mvn test`.

Run a specific test class with `mvn test -Dtest=classname`.

Run a specific test from a specific test class with `mvn test -Dtest=classname#methodname`.